{
  "name": "myworkbench",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first personal work evidence cockpit",
  "author": "MyWorkbench contributors",
  "type": "module",
  "main": "dist-electron/apps/desktop/main.js",
  "scripts": {
    "dev:web": "vite",
    "dev:core": "tsx src/local-api/dev.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:web": "vite build",
    "build:electron": "tsc -p tsconfig.electron.json && tsc -p tsconfig.preload.json",
    "build": "npm run typecheck && npm run build:web && npm run build:electron",
    "start": "electron .",
    "dist:mac": "npm run build && electron-builder --mac --publish never",
    "dist:win": "npm run build && electron-builder --win --publish never",
    "lint": "npm run typecheck"
  },
  "devDependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "@types/node": "24.10.1",
    "@types/react": "19.2.7",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "5.1.1",
    "electron": "43.4.1",
    "electron-builder": "26.15.3",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "vitest": "4.1.11"
  }
}
